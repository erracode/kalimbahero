# Database Connection (PostgreSQL)
# e.g., postgres://user:password@localhost:5432/kalimba_hero
DATABASE_URL=""

# Better Auth Configuration
# Generate a secret with `openssl rand -base64 32` or similar
BETTER_AUTH_SECRET=""
BETTER_AUTH_URL="http://localhost:3000" # Base URL of the auth server
